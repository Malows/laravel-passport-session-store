{
  "name": "@malows/laravel-passport-session-store",
  "version": "1.0.1",
  "description": "Laravel passport session store",
  "main": "dist/index.js",
  "private": false,
  "homepage": "https://github.com/Malows/laravel-passport-session-store",
  "repository": {
    "type": "git",
    "url": "git=https://github.com/Malows/laravel-passport-session-store.git"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=dist/index.js --target=es2017 --tsconfig=tsconfig.json --minify --format=esm --external:date-fns",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "laravel",
    "passport",
    "oauth",
    "vuex"
  ],
  "author": "Juan Manuel Cruz <cruz.jm.stafe@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "date-fns": "^2.19.0"
  },
  "devDependencies": {
    "esbuild": "^0.8.56"
  }
}
